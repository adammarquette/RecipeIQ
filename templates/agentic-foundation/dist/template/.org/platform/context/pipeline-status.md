# Platform — Pipeline Status

## Priorities

1. ~~Add `.NET build + test` workflow~~ — done (`build.yml`)
2. ~~Add test coverage reporting to `build.yml`~~ — done
3. Configure branch protection rules on GitHub for `master` and `develop`
4. Define deployment workflow for target Azure environment (see [Architecture](../../../.docs/architecture.md))
5. Set up secret scanning / dependency vulnerability alerts

